# node_registry_watcher.py — Python dependencies
# Note: The watcher uses only Python stdlib (urllib, json, logging, etc.)
# No external packages are required for basic operation.
#
# Optional dependencies for enhanced functionality:
# requests>=2.28.0  # Alternative to urllib for HTTP requests
# aiohttp>=3.8.0    # Async HTTP client for concurrent node polling
